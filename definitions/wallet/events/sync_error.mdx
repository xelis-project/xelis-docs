### `sync_error`

When the wallet sync loop reports an error.


On Event
```json
{
    "id": 1,
    "jsonrpc": "2.0",
    "result": {
        "event": "sync_error",
        "message": "connection closed"
    }
}
```
