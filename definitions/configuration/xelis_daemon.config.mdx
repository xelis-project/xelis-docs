{/* This file is @generated by crates/xelis-config-docs. */}

## Configuration Reference

XELIS daemon node. Synchronizes and validates the blockchain, participates in the P2P network, and serves RPC and mining work.

### Parameters

- [`--disable-getwork-server`](#xelis-daemon-disable-getwork-server): Disable GetWork Server (WebSocket for miners)
- [`--getwork-rate-limit-ms <getwork-rate-limit-ms>`](#xelis-daemon-getwork-rate-limit-ms): Set the rate limit for GetWork server in milliseconds. In case of high transactions added in mempool, new jobs are rate limited. If is set to 0 (no limit), any new job will be sent to miners directly
- [`--getwork-notify-job-concurrency <getwork-notify-job-concurrency>`](#xelis-daemon-getwork-notify-job-concurrency): Set the concurrency for GetWork server during a new job notification. Notify concurrently to N miners at a time. Set to 0 means no limit and will process as one task per miner. Default is detected based on available parallelism
- [`--enable-prometheus`](#xelis-daemon-enable): Enable Prometheus metrics server This only works if the RPC server is enabled
- [`--prometheus-route <prometheus-route>`](#xelis-daemon-prometheus-route): Route for the Prometheus metrics export
- [`--disable-rpc-server`](#xelis-daemon-disable-rpc-server): Disable RPC Server This will also disable the GetWork Server as it is loaded on RPC server
- [`--rpc-bind-address <rpc-bind-address>`](#xelis-daemon-rpc-bind-address): RPC bind address to listen for HTTP requests
- [`--rpc-threads <rpc-threads>`](#xelis-daemon-rpc-threads): Number of workers to spawn for the HTTP server. If not provided, it will use the available paralellism
- [`--rpc-notify-events-concurrency <rpc-notify-events-concurrency>`](#xelis-daemon-rpc-notify-events-concurrency): RPC Server notification events concurrency This is used to configure the number of concurrent tasks that will be used to notify the events to the clients. By default, it will use the available parallelism. If set to 0, it will be unlimited
- [`--rpc-json-rpc-batch-limit <rpc-json-rpc-batch-limit>`](#xelis-daemon-rpc-json-rpc-batch-limit): Configure the maximum batch size for JSON-RPC requests. This is used to prevent DoS attacks by limiting the number of requests that can be sent in a single batch. Default is 20 requests per batch
- [`--rpc-max-websocket-sessions <rpc-max-websocket-sessions>`](#xelis-daemon-rpc-max-websocket-sessions): Maximum websocket sessions accepted by each RPC websocket server
- [`--rpc-websocket-session-channel-size <rpc-websocket-session-channel-size>`](#xelis-daemon-rpc-websocket-session-channel-size): Maximum outbound messages queued per websocket session
- [`--rpc-websocket-session-work-queue-size <rpc-websocket-session-work-queue-size>`](#xelis-daemon-rpc-websocket-session-work-queue-size): Maximum inbound RPC work queued per websocket session
- [`--rpc-cors-allowed-origins <rpc-cors-allowed-origins>`](#xelis-daemon-rpc-cors-allowed-origins): Configure CORS allowed origins for RPC server. This will allow any whitelisted origin to access the RPC server
- [`--rpc-allow-private-methods`](#xelis-daemon-rpc-allow-private-methods): Allow RPC methods that are private and should not be exposed publicly. This is useful for nodes that are running in a trusted environment and want to expose admin commands
- [`--rpc-allow-contract-vm-executions`](#xelis-daemon-rpc-allow-contract-vm-executions): Allow the contract VM executions in the RPC methods. This is useful for nodes that want to enable the contract VM executions in the RPC methods. Currently used for the `simulate_contract_invoke` method to execute the contract using current chain state
- [`--p2p-proxy-address <p2p-proxy-address>`](#xelis-daemon-p2p-proxy-address): Configure a proxy address to be used Make sure to set the `proxy` type along it when connecting to a peer
- [`--p2p-proxy <p2p-proxy>`](#xelis-daemon-p2p-proxy): Proxy protocol to use when connecting to a peer
- [`--p2p-proxy-username <p2p-proxy-username>`](#xelis-daemon-p2p-proxy-username): Proxy username for authentication
- [`--p2p-proxy-password <p2p-proxy-password>`](#xelis-daemon-p2p-proxy-password): Proxy password for authentication
- [`--tag <TAG>`](#xelis-daemon-tag): Optional node tag This is used to identify the node in the network
- [`--p2p-bind-address <p2p-bind-address>`](#xelis-daemon-p2p-bind-address): P2p bind address to listen for incoming connections
- [`--max-peers <MAX_PEERS>`](#xelis-daemon-max-peers): Number of maximums peers allowed
- [`--p2p-max-outgoing-peers <p2p-max-outgoing-peers>`](#xelis-daemon-p2p-max-outgoing-peers): Set a maximum of P2P outgoing peers.
- [`--priority-nodes <PRIORITY_NODES>`](#xelis-daemon-priority-nodes): Add a priority node to connect when P2p is started. A priority node is connected only one time
- [`--exclusive-nodes <EXCLUSIVE_NODES>`](#xelis-daemon-exclusive-nodes): An exclusive node is connected and its connection is maintained in case of disconnect it also replaces seed nodes. NOTE: no others nodes will be accepted if an exclusive node is set
- [`--disable-p2p-server`](#xelis-daemon-disable-p2p-server): Disable the P2P Server. No connections will be accepted. Node will not be able to communicate the network
- [`--allow-fast-sync`](#xelis-daemon-allow-fast-sync): Allow fast sync mode.
- [`--allow-boost-sync`](#xelis-daemon-allow-boost-sync): Allow boost chain sync mode.
- [`--allow-priority-blocks`](#xelis-daemon-allow-priority-blocks): Allow blocks coming from priority nodes to be fast forwarded to our peers.
- [`--max-chain-response-size <MAX_CHAIN_RESPONSE_SIZE>`](#xelis-daemon-max-chain-response-size): Configure the maximum chain response size.
- [`--disable-ip-sharing`](#xelis-daemon-disable-ip-sharing): Ask peers to not share our IP to others and/or through API.
- [`--p2p-concurrency-task-count-limit <p2p-concurrency-task-count-limit>`](#xelis-daemon-p2p-concurrency-task-count-limit): Limit of concurrent tasks accepting new incoming connections
- [`--p2p-on-dh-key-change <p2p-on-dh-key-change>`](#xelis-daemon-p2p-on-dh-key-change): Execute a specific action when the P2p Diffie-Hellman Key of a peer is different from our stored one. By default, it will ignore the key change and update it
- [`--p2p-dh-private-key <p2p-dh-private-key>`](#xelis-daemon-p2p-dh-private-key): P2p DH private key to use. By default, a newly generated key will be used. Reusing the same private key will allow to keep the same public key and avoid the need to re-verify the key with our peers. This is useful for nodes that want to keep the same public key across several restarts. Note that reusing the same key may allow to track your node across your IP changes
- [`--p2p-stream-concurrency <p2p-stream-concurrency>`](#xelis-daemon-p2p-stream-concurrency): P2P Concurrency to use during streams. This is used to configure the number of concurrent tasks that will be used to process the streams. By default, it will use the available parallelism. If set to 0, it will be unlimited
- [`--p2p-temp-ban-duration <p2p-temp-ban-duration>`](#xelis-daemon-p2p-temp-ban-duration): P2P Time to set when banning a peer temporarily due to the fail count limit reached. This is used to configure the time to wait before unbanning the peer
- [`--p2p-fail-count-limit <p2p-fail-count-limit>`](#xelis-daemon-p2p-fail-count-limit): P2P Fail count limit to ban a peer temporarily. This is used to configure the number of failed requests before banning the peer temporarily
- [`--disable-p2p-reexecute-blocks-on-sync`](#xelis-daemon-disable-p2p-reexecute-blocks-on-sync): Disable the P2P to re-execute an orphaned block during chain sync. If set to true, the P2P server will stop removing the block from storage and prevent to re-execute it by re-adding it to the chain
- [`--p2p-block-propagation-log-level <p2p-block-propagation-log-level>`](#xelis-daemon-p2p-block-propagation-log-level): P2P log level for the block propagation This is used to configure the log level used during the block propagation to peers. By default, it will be set to "debug"
- [`--disable-p2p-fetching-txs-propagated`](#xelis-daemon-disable-p2p-fetching-txs-propagated): Disable requesting P2P transactions propagated. No transaction being broadcasted across the p2p network will be requested. This may leads to mempool being not synchronized at all. It is discouraged to enable this config in a small p2p network
- [`--p2p-handle-peer-packets-in-dedicated-task`](#xelis-daemon-p2p-handle-peer-packets-in-dedicated-task): Handle peer packets in parallel by creating a new dedicated task. Each packet has its own dedicated task expect those which are order dependent. They are set in an sequential executor to ensure that the order stay the same despite this config enabled. Creating a dedicated task per packet handling is useful for reducing latency during heavy network usage but may increase heavily the network usage under high load. By default, all p2p packets are handled sequentially in a single task per peer
- [`--enable-p2p-compression`](#xelis-daemon-enable-p2p-compression): Experimental: Enable the compression for packets being sent to peers. Compression is done using the Snappy algorithm. It is only used for packets greater than 1 KiB. This is useful to reduce the bandwidth usage when having several peers. Note that it may increase the CPU usage due to the compression/decompression. By default, it is disabled
- [`--disable-fast-sync-support`](#xelis-daemon-disable-fast-sync-support): Disable the fast sync support. If set to true, others nodes will not be able to use the fast sync mode with us
- [`--p2p-sync-from-priority-only`](#xelis-daemon-p2p-sync-from-priority-only): During the sync from peers, only sync from priority nodes. This is useful to ensure that we only sync from trusted nodes
- [`--p2p-reorg-from-priority-only`](#xelis-daemon-p2p-reorg-from-priority-only): During a reorg, only accept blocks from priority nodes. This is useful to ensure that we only accept blocks from trusted nodes
- [`--mempool-min-fee-per-kb <MIN_FEE_PER_KB>`](#xelis-daemon-min-fee-per-kb): Minimum fee per kB to consider a transaction as valid for the mempool. This is in atomic units (1e-8 of the currency unit)
- [`--mempool-tx-expiration-time <TX_EXPIRATION_TIME>`](#xelis-daemon-tx-expiration-time): Maximum time a transaction can stay in the mempool before being considered as expired and removed
- [`--mempool-max-memory-usage <MAX_MEMORY_USAGE>`](#xelis-daemon-max-memory-usage): Maximum mempool memory usage before starting to evict transactions
- [`--dir-path <DIR_PATH>`](#xelis-daemon-dir-path): Set dir path for blockchain storage. This will be appended by the network name for the database directory. It must ends with a slash
- [`--simulator <SIMULATOR>`](#xelis-daemon-simulator): Enable the simulator (skip PoW verification, generate a new block for every BLOCK_TIME)
- [`--skip-pow-verification`](#xelis-daemon-skip-pow-verification): Skip PoW verification. Warning: This is dangerous and should not be used in production
- [`--enable-contracts-logging`](#xelis-daemon-enable-contracts-logging): Enable the contracts logging during their execution. This will print the logs of the contracts being executed in the block
- [`--auto-prune-keep-n-blocks <AUTO_PRUNE_KEEP_N_BLOCKS>`](#xelis-daemon-auto-prune-keep-n-blocks): Enable the auto prune mode and prune the chain at each new block by keeping at least N blocks before the top
- [`--skip-block-template-txs-verification`](#xelis-daemon-skip-block-template-txs-verification): Skip the TXs verification when building a block template
- [`--genesis-block-hex <GENESIS_BLOCK_HEX>`](#xelis-daemon-genesis-block-hex): Use the hexadecimal representation of the genesis block for the dev mode. This is useful for testing and development
- [`<checkpoints>`](#xelis-daemon-checkpoints): Blocks hashes checkpoints No rewind can go below any of those checkpoints
- [`--txs-verification-threads-count <TXS_VERIFICATION_THREADS_COUNT>`](#xelis-daemon-txs-verification-threads-count): Set the threads count to use during TXs verifications. By default, will detect the best value. If set to 1, it will use the main thread
- [`--pre-verify-block-threads-count <PRE_VERIFY_BLOCK_THREADS_COUNT>`](#xelis-daemon-pre-verify-block-threads-count): Se the threads count to use during block pre verification. By default, will detect the best value
- [`--check-db-integrity`](#xelis-daemon-check-db-integrity): Enable the DB integrity check that happen on chain initialization. This may take some times on huge DB as it's iterating through all versioned data to verify that no pointer or version is above our current topoheight. Please note that if the recovery mode is enabled, this will be skipped. By default, it is disabled
- [`--recovery-mode`](#xelis-daemon-recovery-mode): Enable the recovery mode of the daemon. No DB integrity check or pre-computations will occurs such as difficulty for tips, stable height, etc
- [`--flush-db-every-n-blocks <FLUSH_DB_EVERY_N_BLOCKS>`](#xelis-daemon-flush-db-every-n-blocks): Flush the storage onto the disk every N blocks (topoheight based). In case of RocksDB, this will also compact the changes
- [`--disable-zkp-cache`](#xelis-daemon-disable-zkp-cache): Disable the TX Cache (ZKP Cache) ZKP Cache is enabled by default and prevent to re-verify the same ZK Proofs more than once
- [`--concurrency <CONCURRENCY>`](#xelis-daemon-concurrency): Max concurrency allowed for general tasks By default, it will use the available parallelism
- [`--enable-snapshot-on-reorg`](#xelis-daemon-enable-snapshot-on-reorg): Enable snapshot mode during DAG reorganizations. This will create a snapshot of the current state before applying the reorg and will use as a memory buffer to apply the reorg and then flush it to the storage at the end of the reorg
- [`--sled-cache-size <sled-cache-size>`](#xelis-daemon-sled-cache-size): Set LRUCache size (0 = disabled)
- [`--sled-internal-cache-size <sled-internal-cache-size>`](#xelis-daemon-sled-internal-cache-size): DB cache size in bytes
- [`--sled-internal-db-mode <sled-internal-db-mode>`](#xelis-daemon-sled-internal-db-mode): Internal DB mode to use
- [`--use-db-backend <USE_DB_BACKEND>`](#xelis-daemon-use-db-backend): Storage backend used by the daemon.
- [`--log-level <LOG_LEVEL>`](#xelis-daemon-log-level): Minimum log level printed to the terminal
- [`--file-log-level <FILE_LOG_LEVEL>`](#xelis-daemon-file-log-level): Minimum log level written to the log file.
- [`--disable-file-logging`](#xelis-daemon-disable-file-logging): Disable writing logs to a file
- [`--disable-file-log-date-based`](#xelis-daemon-disable-file-log-date-based): Disable date-based log filenames.
- [`--disable-log-color`](#xelis-daemon-disable-log-color): Disable colors in terminal logs
- [`--disable-interactive-mode`](#xelis-daemon-disable-interactive-mode): Disable the interactive terminal prompt.
- [`--auto-compress-logs`](#xelis-daemon-auto-compress-logs): Compress rotated log files automatically.
- [`--filename-log <FILENAME_LOG>`](#xelis-daemon-filename-log): Log filename stored inside `logs_path`.
- [`--logs-path <LOGS_PATH>`](#xelis-daemon-logs-path): Directory where log files are written.
- [`--logs-modules <LOGS_MODULES>`](#xelis-daemon-logs-modules): Per-module log filters
- [`--disable-ascii-art`](#xelis-daemon-disable-ascii-art): Disable the ASCII art shown at startup
- [`--datetime-format <DATETIME_FORMAT>`](#xelis-daemon-datetime-format): Datetime format used in log entries
- [`--network <NETWORK>`](#xelis-daemon-network): Network to run and validate
- [`--config-file <CONFIG_FILE>`](#xelis-daemon-config-file): JSON configuration file to load
- [`--generate-config-template`](#xelis-daemon-generate-config-template): Generate a configuration template at `config_file`

<a id="xelis-daemon-disable-getwork-server"></a>
#### `--disable-getwork-server`

Disable GetWork Server (WebSocket for miners)

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-getwork-server` |
| Required | No |
| Value | No |
| Value name | `disable-getwork-server` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-getwork-rate-limit-ms"></a>
#### `--getwork-rate-limit-ms <getwork-rate-limit-ms>`

Set the rate limit for GetWork server in milliseconds. In case of high transactions added in mempool, new jobs are rate limited. If is set to 0 (no limit), any new job will be sent to miners directly

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-getwork-rate-limit-ms` |
| Required | No |
| Value | Yes |
| Value name | `getwork-rate-limit-ms` |
| Default value | `500` |

<a id="xelis-daemon-getwork-notify-job-concurrency"></a>
#### `--getwork-notify-job-concurrency <getwork-notify-job-concurrency>`

Set the concurrency for GetWork server during a new job notification. Notify concurrently to N miners at a time. Set to 0 means no limit and will process as one task per miner. Default is detected based on available parallelism

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-getwork-notify-job-concurrency` |
| Required | No |
| Value | Yes |
| Value name | `getwork-notify-job-concurrency` |
| Default value | `32` |

<a id="xelis-daemon-enable"></a>
#### `--enable-prometheus`

Enable Prometheus metrics server This only works if the RPC server is enabled

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-enable` |
| Required | No |
| Value | No |
| Value name | `ENABLE` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-prometheus-route"></a>
#### `--prometheus-route <prometheus-route>`

Route for the Prometheus metrics export

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-prometheus-route` |
| Required | No |
| Value | Yes |
| Value name | `prometheus-route` |
| Default value | `/metrics` |

<a id="xelis-daemon-disable-rpc-server"></a>
#### `--disable-rpc-server`

Disable RPC Server This will also disable the GetWork Server as it is loaded on RPC server

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-rpc-server` |
| Required | No |
| Value | No |
| Value name | `disable-rpc-server` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-rpc-bind-address"></a>
#### `--rpc-bind-address <rpc-bind-address>`

RPC bind address to listen for HTTP requests

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-bind-address` |
| Required | No |
| Value | Yes |
| Value name | `rpc-bind-address` |
| Default value | `0.0.0.0:8080` |

<a id="xelis-daemon-rpc-threads"></a>
#### `--rpc-threads <rpc-threads>`

Number of workers to spawn for the HTTP server. If not provided, it will use the available paralellism

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-threads` |
| Required | No |
| Value | Yes |
| Value name | `rpc-threads` |
| Default value | `32` |

<a id="xelis-daemon-rpc-notify-events-concurrency"></a>
#### `--rpc-notify-events-concurrency <rpc-notify-events-concurrency>`

RPC Server notification events concurrency This is used to configure the number of concurrent tasks that will be used to notify the events to the clients. By default, it will use the available parallelism. If set to 0, it will be unlimited

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-notify-events-concurrency` |
| Required | No |
| Value | Yes |
| Value name | `rpc-notify-events-concurrency` |
| Default value | `32` |

<a id="xelis-daemon-rpc-json-rpc-batch-limit"></a>
#### `--rpc-json-rpc-batch-limit <rpc-json-rpc-batch-limit>`

Configure the maximum batch size for JSON-RPC requests. This is used to prevent DoS attacks by limiting the number of requests that can be sent in a single batch. Default is 20 requests per batch

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-json-rpc-batch-limit` |
| Required | No |
| Value | Yes |
| Value name | `rpc-json-rpc-batch-limit` |
| Default value | `20` |

<a id="xelis-daemon-rpc-max-websocket-sessions"></a>
#### `--rpc-max-websocket-sessions <rpc-max-websocket-sessions>`

Maximum websocket sessions accepted by each RPC websocket server

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-max-websocket-sessions` |
| Required | No |
| Value | Yes |
| Value name | `rpc-max-websocket-sessions` |
| Default value | `1024` |

<a id="xelis-daemon-rpc-websocket-session-channel-size"></a>
#### `--rpc-websocket-session-channel-size <rpc-websocket-session-channel-size>`

Maximum outbound messages queued per websocket session

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-websocket-session-channel-size` |
| Required | No |
| Value | Yes |
| Value name | `rpc-websocket-session-channel-size` |
| Default value | `256` |

<a id="xelis-daemon-rpc-websocket-session-work-queue-size"></a>
#### `--rpc-websocket-session-work-queue-size <rpc-websocket-session-work-queue-size>`

Maximum inbound RPC work queued per websocket session

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-websocket-session-work-queue-size` |
| Required | No |
| Value | Yes |
| Value name | `rpc-websocket-session-work-queue-size` |
| Default value | `64` |

<a id="xelis-daemon-rpc-cors-allowed-origins"></a>
#### `--rpc-cors-allowed-origins <rpc-cors-allowed-origins>`

Configure CORS allowed origins for RPC server. This will allow any whitelisted origin to access the RPC server

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-cors-allowed-origins` |
| Required | No |
| Value | Yes |
| Value name | `rpc-cors-allowed-origins` |
| Multiple | Yes |

<a id="xelis-daemon-rpc-allow-private-methods"></a>
#### `--rpc-allow-private-methods`

Allow RPC methods that are private and should not be exposed publicly. This is useful for nodes that are running in a trusted environment and want to expose admin commands

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-allow-private-methods` |
| Required | No |
| Value | No |
| Value name | `rpc-allow-private-methods` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-rpc-allow-contract-vm-executions"></a>
#### `--rpc-allow-contract-vm-executions`

Allow the contract VM executions in the RPC methods. This is useful for nodes that want to enable the contract VM executions in the RPC methods. Currently used for the `simulate_contract_invoke` method to execute the contract using current chain state

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-rpc-allow-contract-vm-executions` |
| Required | No |
| Value | No |
| Value name | `rpc-allow-contract-vm-executions` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-proxy-address"></a>
#### `--p2p-proxy-address <p2p-proxy-address>`

Configure a proxy address to be used Make sure to set the `proxy` type along it when connecting to a peer

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-proxy-address` |
| Required | No |
| Value | Yes |
| Value name | `p2p-proxy-address` |

<a id="xelis-daemon-p2p-proxy"></a>
#### `--p2p-proxy <p2p-proxy>`

Proxy protocol to use when connecting to a peer

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-proxy` |
| Required | No |
| Value | Yes |
| Value name | `p2p-proxy` |
| Possible values | `socks5`, `socks4` |

<a id="xelis-daemon-p2p-proxy-username"></a>
#### `--p2p-proxy-username <p2p-proxy-username>`

Proxy username for authentication

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-proxy-username` |
| Required | No |
| Value | Yes |
| Value name | `p2p-proxy-username` |

<a id="xelis-daemon-p2p-proxy-password"></a>
#### `--p2p-proxy-password <p2p-proxy-password>`

Proxy password for authentication

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-proxy-password` |
| Required | No |
| Value | Yes |
| Value name | `p2p-proxy-password` |

<a id="xelis-daemon-tag"></a>
#### `--tag <TAG>`

Optional node tag This is used to identify the node in the network

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-tag` |
| Required | No |
| Value | Yes |
| Value name | `TAG` |

<a id="xelis-daemon-p2p-bind-address"></a>
#### `--p2p-bind-address <p2p-bind-address>`

P2p bind address to listen for incoming connections

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-bind-address` |
| Required | No |
| Value | Yes |
| Value name | `p2p-bind-address` |
| Default value | `0.0.0.0:2125` |

<a id="xelis-daemon-max-peers"></a>
#### `--max-peers <MAX_PEERS>`

Number of maximums peers allowed

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-max-peers` |
| Required | No |
| Value | Yes |
| Value name | `MAX_PEERS` |
| Default value | `32` |

<a id="xelis-daemon-p2p-max-outgoing-peers"></a>
#### `--p2p-max-outgoing-peers <p2p-max-outgoing-peers>`

Set a maximum of P2P outgoing peers.

This is useful to limit to how many nodes you want to connect to.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-max-outgoing-peers` |
| Required | No |
| Value | Yes |
| Value name | `p2p-max-outgoing-peers` |
| Default value | `8` |

<a id="xelis-daemon-priority-nodes"></a>
#### `--priority-nodes <PRIORITY_NODES>`

Add a priority node to connect when P2p is started. A priority node is connected only one time

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-priority-nodes` |
| Required | No |
| Value | Yes |
| Value name | `PRIORITY_NODES` |
| Multiple | Yes |

<a id="xelis-daemon-exclusive-nodes"></a>
#### `--exclusive-nodes <EXCLUSIVE_NODES>`

An exclusive node is connected and its connection is maintained in case of disconnect it also replaces seed nodes. NOTE: no others nodes will be accepted if an exclusive node is set

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-exclusive-nodes` |
| Required | No |
| Value | Yes |
| Value name | `EXCLUSIVE_NODES` |
| Multiple | Yes |

<a id="xelis-daemon-disable-p2p-server"></a>
#### `--disable-p2p-server`

Disable the P2P Server. No connections will be accepted. Node will not be able to communicate the network

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-p2p-server` |
| Required | No |
| Value | No |
| Value name | `disable-p2p-server` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-allow-fast-sync"></a>
#### `--allow-fast-sync`

Allow fast sync mode.

Sync a bootstrapped chain if your local copy is outdated.

It will not store any blocks / TXs and will not verify the history locally.

Use it with extreme cautions and trusted nodes to have a valid bootstrapped chain.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-allow-fast-sync` |
| Required | No |
| Value | No |
| Value name | `ALLOW_FAST_SYNC` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-allow-boost-sync"></a>
#### `--allow-boost-sync`

Allow boost chain sync mode.

This will request in parallel all blocks instead of sequentially.

It is not enabled by default because it will requests several blocks before validating each previous.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-allow-boost-sync` |
| Required | No |
| Value | No |
| Value name | `ALLOW_BOOST_SYNC` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-allow-priority-blocks"></a>
#### `--allow-priority-blocks`

Allow blocks coming from priority nodes to be fast forwarded to our peers.

Propagate a new block to our peers as soon as we receive it from a priority node before verifying it ourself. This reduces the time to propagate a new block to our peers. Useful for pools operating having several nodes across the world to propagate their blocks faster.

By default, this is disabled.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-allow-priority-blocks` |
| Required | No |
| Value | No |
| Value name | `ALLOW_PRIORITY_BLOCKS` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-max-chain-response-size"></a>
#### `--max-chain-response-size <MAX_CHAIN_RESPONSE_SIZE>`

Configure the maximum chain response size.

This is useful for low devices who want to reduce resources usage and for high-end devices who want to (or help others to) sync faster.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-max-chain-response-size` |
| Required | No |
| Value | Yes |
| Value name | `MAX_CHAIN_RESPONSE_SIZE` |
| Default value | `4096` |

<a id="xelis-daemon-disable-ip-sharing"></a>
#### `--disable-ip-sharing`

Ask peers to not share our IP to others and/or through API.

This is useful for people that don't want that their IP is revealed in RPC API and/or shared to others nodes as a potential new peer to connect to.

Note that it may prevent to have new incoming peers.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-ip-sharing` |
| Required | No |
| Value | No |
| Value name | `DISABLE_IP_SHARING` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-concurrency-task-count-limit"></a>
#### `--p2p-concurrency-task-count-limit <p2p-concurrency-task-count-limit>`

Limit of concurrent tasks accepting new incoming connections

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-concurrency-task-count-limit` |
| Required | No |
| Value | Yes |
| Value name | `p2p-concurrency-task-count-limit` |
| Default value | `4` |

<a id="xelis-daemon-p2p-on-dh-key-change"></a>
#### `--p2p-on-dh-key-change <p2p-on-dh-key-change>`

Execute a specific action when the P2p Diffie-Hellman Key of a peer is different from our stored one. By default, it will ignore the key change and update it

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-on-dh-key-change` |
| Required | No |
| Value | Yes |
| Value name | `p2p-on-dh-key-change` |
| Default value | `ignore` |
| Possible values | `warn`, `reject`, `ignore` |

<a id="xelis-daemon-p2p-dh-private-key"></a>
#### `--p2p-dh-private-key <p2p-dh-private-key>`

P2p DH private key to use. By default, a newly generated key will be used. Reusing the same private key will allow to keep the same public key and avoid the need to re-verify the key with our peers. This is useful for nodes that want to keep the same public key across several restarts. Note that reusing the same key may allow to track your node across your IP changes

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-dh-private-key` |
| Required | No |
| Value | Yes |
| Value name | `p2p-dh-private-key` |

<a id="xelis-daemon-p2p-stream-concurrency"></a>
#### `--p2p-stream-concurrency <p2p-stream-concurrency>`

P2P Concurrency to use during streams. This is used to configure the number of concurrent tasks that will be used to process the streams. By default, it will use the available parallelism. If set to 0, it will be unlimited

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-stream-concurrency` |
| Required | No |
| Value | Yes |
| Value name | `p2p-stream-concurrency` |
| Default value | `32` |

<a id="xelis-daemon-p2p-temp-ban-duration"></a>
#### `--p2p-temp-ban-duration <p2p-temp-ban-duration>`

P2P Time to set when banning a peer temporarily due to the fail count limit reached. This is used to configure the time to wait before unbanning the peer

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-temp-ban-duration` |
| Required | No |
| Value | Yes |
| Value name | `p2p-temp-ban-duration` |
| Default value | `15m` |

<a id="xelis-daemon-p2p-fail-count-limit"></a>
#### `--p2p-fail-count-limit <p2p-fail-count-limit>`

P2P Fail count limit to ban a peer temporarily. This is used to configure the number of failed requests before banning the peer temporarily

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-fail-count-limit` |
| Required | No |
| Value | Yes |
| Value name | `p2p-fail-count-limit` |
| Default value | `50` |

<a id="xelis-daemon-disable-p2p-reexecute-blocks-on-sync"></a>
#### `--disable-p2p-reexecute-blocks-on-sync`

Disable the P2P to re-execute an orphaned block during chain sync. If set to true, the P2P server will stop removing the block from storage and prevent to re-execute it by re-adding it to the chain

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-p2p-reexecute-blocks-on-sync` |
| Required | No |
| Value | No |
| Value name | `disable-p2p-reexecute-blocks-on-sync` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-block-propagation-log-level"></a>
#### `--p2p-block-propagation-log-level <p2p-block-propagation-log-level>`

P2P log level for the block propagation This is used to configure the log level used during the block propagation to peers. By default, it will be set to "debug"

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-block-propagation-log-level` |
| Required | No |
| Value | Yes |
| Value name | `p2p-block-propagation-log-level` |
| Default value | `debug` |
| Possible values | `off`, `error`, `warn`, `info`, `debug`, `trace` |

<a id="xelis-daemon-disable-p2p-fetching-txs-propagated"></a>
#### `--disable-p2p-fetching-txs-propagated`

Disable requesting P2P transactions propagated. No transaction being broadcasted across the p2p network will be requested. This may leads to mempool being not synchronized at all. It is discouraged to enable this config in a small p2p network

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-p2p-fetching-txs-propagated` |
| Required | No |
| Value | No |
| Value name | `disable-p2p-fetching-txs-propagated` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-handle-peer-packets-in-dedicated-task"></a>
#### `--p2p-handle-peer-packets-in-dedicated-task`

Handle peer packets in parallel by creating a new dedicated task. Each packet has its own dedicated task expect those which are order dependent. They are set in an sequential executor to ensure that the order stay the same despite this config enabled. Creating a dedicated task per packet handling is useful for reducing latency during heavy network usage but may increase heavily the network usage under high load. By default, all p2p packets are handled sequentially in a single task per peer

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-handle-peer-packets-in-dedicated-task` |
| Required | No |
| Value | No |
| Value name | `p2p-handle-peer-packets-in-dedicated-task` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-enable-p2p-compression"></a>
#### `--enable-p2p-compression`

Experimental: Enable the compression for packets being sent to peers. Compression is done using the Snappy algorithm. It is only used for packets greater than 1 KiB. This is useful to reduce the bandwidth usage when having several peers. Note that it may increase the CPU usage due to the compression/decompression. By default, it is disabled

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-enable-p2p-compression` |
| Required | No |
| Value | No |
| Value name | `enable-p2p-compression` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-disable-fast-sync-support"></a>
#### `--disable-fast-sync-support`

Disable the fast sync support. If set to true, others nodes will not be able to use the fast sync mode with us

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-fast-sync-support` |
| Required | No |
| Value | No |
| Value name | `disable-fast-sync-support` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-sync-from-priority-only"></a>
#### `--p2p-sync-from-priority-only`

During the sync from peers, only sync from priority nodes. This is useful to ensure that we only sync from trusted nodes

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-sync-from-priority-only` |
| Required | No |
| Value | No |
| Value name | `p2p-sync-from-priority-only` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-p2p-reorg-from-priority-only"></a>
#### `--p2p-reorg-from-priority-only`

During a reorg, only accept blocks from priority nodes. This is useful to ensure that we only accept blocks from trusted nodes

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-p2p-reorg-from-priority-only` |
| Required | No |
| Value | No |
| Value name | `p2p-reorg-from-priority-only` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-min-fee-per-kb"></a>
#### `--mempool-min-fee-per-kb <MIN_FEE_PER_KB>`

Minimum fee per kB to consider a transaction as valid for the mempool. This is in atomic units (1e-8 of the currency unit)

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-min-fee-per-kb` |
| Required | No |
| Value | Yes |
| Value name | `MIN_FEE_PER_KB` |
| Default value | `10000` |

<a id="xelis-daemon-tx-expiration-time"></a>
#### `--mempool-tx-expiration-time <TX_EXPIRATION_TIME>`

Maximum time a transaction can stay in the mempool before being considered as expired and removed

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-tx-expiration-time` |
| Required | No |
| Value | Yes |
| Value name | `TX_EXPIRATION_TIME` |
| Default value | `1day` |

<a id="xelis-daemon-max-memory-usage"></a>
#### `--mempool-max-memory-usage <MAX_MEMORY_USAGE>`

Maximum mempool memory usage before starting to evict transactions

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-max-memory-usage` |
| Required | No |
| Value | Yes |
| Value name | `MAX_MEMORY_USAGE` |
| Default value | `800000000` |

<a id="xelis-daemon-dir-path"></a>
#### `--dir-path <DIR_PATH>`

Set dir path for blockchain storage. This will be appended by the network name for the database directory. It must ends with a slash

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-dir-path` |
| Required | No |
| Value | Yes |
| Value name | `DIR_PATH` |

<a id="xelis-daemon-simulator"></a>
#### `--simulator <SIMULATOR>`

Enable the simulator (skip PoW verification, generate a new block for every BLOCK_TIME)

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-simulator` |
| Required | No |
| Value | Yes |
| Value name | `SIMULATOR` |

<a id="xelis-daemon-skip-pow-verification"></a>
#### `--skip-pow-verification`

Skip PoW verification. Warning: This is dangerous and should not be used in production

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-skip-pow-verification` |
| Required | No |
| Value | No |
| Value name | `SKIP_POW_VERIFICATION` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-enable-contracts-logging"></a>
#### `--enable-contracts-logging`

Enable the contracts logging during their execution. This will print the logs of the contracts being executed in the block

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-enable-contracts-logging` |
| Required | No |
| Value | No |
| Value name | `ENABLE_CONTRACTS_LOGGING` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-auto-prune-keep-n-blocks"></a>
#### `--auto-prune-keep-n-blocks <AUTO_PRUNE_KEEP_N_BLOCKS>`

Enable the auto prune mode and prune the chain at each new block by keeping at least N blocks before the top

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-auto-prune-keep-n-blocks` |
| Required | No |
| Value | Yes |
| Value name | `AUTO_PRUNE_KEEP_N_BLOCKS` |

<a id="xelis-daemon-skip-block-template-txs-verification"></a>
#### `--skip-block-template-txs-verification`

Skip the TXs verification when building a block template

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-skip-block-template-txs-verification` |
| Required | No |
| Value | No |
| Value name | `SKIP_BLOCK_TEMPLATE_TXS_VERIFICATION` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-genesis-block-hex"></a>
#### `--genesis-block-hex <GENESIS_BLOCK_HEX>`

Use the hexadecimal representation of the genesis block for the dev mode. This is useful for testing and development

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-genesis-block-hex` |
| Required | No |
| Value | Yes |
| Value name | `GENESIS_BLOCK_HEX` |

<a id="xelis-daemon-checkpoints"></a>
#### `<checkpoints>`

Blocks hashes checkpoints No rewind can go below any of those checkpoints

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-checkpoints` |
| Required | No |
| Value | Yes |
| Value name | `CHECKPOINTS` |
| Multiple | Yes |

<a id="xelis-daemon-txs-verification-threads-count"></a>
#### `--txs-verification-threads-count <TXS_VERIFICATION_THREADS_COUNT>`

Set the threads count to use during TXs verifications. By default, will detect the best value. If set to 1, it will use the main thread

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-txs-verification-threads-count` |
| Required | No |
| Value | Yes |
| Value name | `TXS_VERIFICATION_THREADS_COUNT` |
| Default value | `32` |

<a id="xelis-daemon-pre-verify-block-threads-count"></a>
#### `--pre-verify-block-threads-count <PRE_VERIFY_BLOCK_THREADS_COUNT>`

Se the threads count to use during block pre verification. By default, will detect the best value

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-pre-verify-block-threads-count` |
| Required | No |
| Value | Yes |
| Value name | `PRE_VERIFY_BLOCK_THREADS_COUNT` |
| Default value | `32` |

<a id="xelis-daemon-check-db-integrity"></a>
#### `--check-db-integrity`

Enable the DB integrity check that happen on chain initialization. This may take some times on huge DB as it's iterating through all versioned data to verify that no pointer or version is above our current topoheight. Please note that if the recovery mode is enabled, this will be skipped. By default, it is disabled

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-check-db-integrity` |
| Required | No |
| Value | No |
| Value name | `CHECK_DB_INTEGRITY` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-recovery-mode"></a>
#### `--recovery-mode`

Enable the recovery mode of the daemon. No DB integrity check or pre-computations will occurs such as difficulty for tips, stable height, etc

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-recovery-mode` |
| Required | No |
| Value | No |
| Value name | `RECOVERY_MODE` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-flush-db-every-n-blocks"></a>
#### `--flush-db-every-n-blocks <FLUSH_DB_EVERY_N_BLOCKS>`

Flush the storage onto the disk every N blocks (topoheight based). In case of RocksDB, this will also compact the changes

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-flush-db-every-n-blocks` |
| Required | No |
| Value | Yes |
| Value name | `FLUSH_DB_EVERY_N_BLOCKS` |

<a id="xelis-daemon-disable-zkp-cache"></a>
#### `--disable-zkp-cache`

Disable the TX Cache (ZKP Cache) ZKP Cache is enabled by default and prevent to re-verify the same ZK Proofs more than once

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-zkp-cache` |
| Required | No |
| Value | No |
| Value name | `DISABLE_ZKP_CACHE` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-concurrency"></a>
#### `--concurrency <CONCURRENCY>`

Max concurrency allowed for general tasks By default, it will use the available parallelism

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-concurrency` |
| Required | No |
| Value | Yes |
| Value name | `CONCURRENCY` |
| Default value | `32` |

<a id="xelis-daemon-enable-snapshot-on-reorg"></a>
#### `--enable-snapshot-on-reorg`

Enable snapshot mode during DAG reorganizations. This will create a snapshot of the current state before applying the reorg and will use as a memory buffer to apply the reorg and then flush it to the storage at the end of the reorg

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-enable-snapshot-on-reorg` |
| Required | No |
| Value | No |
| Value name | `ENABLE_SNAPSHOT_ON_REORG` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-sled-cache-size"></a>
#### `--sled-cache-size <sled-cache-size>`

Set LRUCache size (0 = disabled)

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-sled-cache-size` |
| Required | No |
| Value | Yes |
| Value name | `sled-cache-size` |
| Default value | `1024` |

<a id="xelis-daemon-sled-internal-cache-size"></a>
#### `--sled-internal-cache-size <sled-internal-cache-size>`

DB cache size in bytes

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-sled-internal-cache-size` |
| Required | No |
| Value | Yes |
| Value name | `sled-internal-cache-size` |
| Default value | `67108864` |

<a id="xelis-daemon-sled-internal-db-mode"></a>
#### `--sled-internal-db-mode <sled-internal-db-mode>`

Internal DB mode to use

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-sled-internal-db-mode` |
| Required | No |
| Value | Yes |
| Value name | `sled-internal-db-mode` |
| Default value | `low-space` |
| Possible values | `high-throughput`, `low-space` |

<a id="xelis-daemon-use-db-backend"></a>
#### `--use-db-backend <USE_DB_BACKEND>`

Storage backend used by the daemon.

Existing data is not migrated when switching backends. Select the backend that matches the data directory you intend to use.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-use-db-backend` |
| Required | No |
| Value | Yes |
| Value name | `USE_DB_BACKEND` |
| Default value | `sled` |
| Possible values | `sled`, `memory` |

<a id="xelis-daemon-log-level"></a>
#### `--log-level <LOG_LEVEL>`

Minimum log level printed to the terminal

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-log-level` |
| Required | No |
| Value | Yes |
| Value name | `LOG_LEVEL` |
| Default value | `info` |
| Possible values | `off`, `error`, `warn`, `info`, `debug`, `trace` |

<a id="xelis-daemon-file-log-level"></a>
#### `--file-log-level <FILE_LOG_LEVEL>`

Minimum log level written to the log file.

Defaults to the terminal log level.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-file-log-level` |
| Required | No |
| Value | Yes |
| Value name | `FILE_LOG_LEVEL` |
| Possible values | `off`, `error`, `warn`, `info`, `debug`, `trace` |

<a id="xelis-daemon-disable-file-logging"></a>
#### `--disable-file-logging`

Disable writing logs to a file

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-file-logging` |
| Required | No |
| Value | No |
| Value name | `DISABLE_FILE_LOGGING` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-disable-file-log-date-based"></a>
#### `--disable-file-log-date-based`

Disable date-based log filenames.

When enabled, the log file is named `xelis-daemon.log` instead of `YYYY-MM-DD.xelis-daemon.log`.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-file-log-date-based` |
| Required | No |
| Value | No |
| Value name | `DISABLE_FILE_LOG_DATE_BASED` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-disable-log-color"></a>
#### `--disable-log-color`

Disable colors in terminal logs

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-log-color` |
| Required | No |
| Value | No |
| Value name | `DISABLE_LOG_COLOR` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-disable-interactive-mode"></a>
#### `--disable-interactive-mode`

Disable the interactive terminal prompt.

CLI commands and live prompt updates are unavailable in this mode.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-interactive-mode` |
| Required | No |
| Value | No |
| Value name | `DISABLE_INTERACTIVE_MODE` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-auto-compress-logs"></a>
#### `--auto-compress-logs`

Compress rotated log files automatically.

This only applies when file logging is enabled.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-auto-compress-logs` |
| Required | No |
| Value | No |
| Value name | `AUTO_COMPRESS_LOGS` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-filename-log"></a>
#### `--filename-log <FILENAME_LOG>`

Log filename stored inside `logs_path`.

Defaults to `xelis-daemon.log`. Unless date-based filenames are disabled, daily log files are named `YYYY-MM-DD.xelis-daemon.log`.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-filename-log` |
| Required | No |
| Value | Yes |
| Value name | `FILENAME_LOG` |
| Default value | `xelis-daemon.log` |

<a id="xelis-daemon-logs-path"></a>
#### `--logs-path <LOGS_PATH>`

Directory where log files are written.

Defaults to `logs/`. The path must end with `/` or `\`.

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-logs-path` |
| Required | No |
| Value | Yes |
| Value name | `LOGS_PATH` |
| Default value | `logs/` |

<a id="xelis-daemon-logs-modules"></a>
#### `--logs-modules <LOGS_MODULES>`

Per-module log filters

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-logs-modules` |
| Required | No |
| Value | Yes |
| Value name | `LOGS_MODULES` |
| Multiple | Yes |

<a id="xelis-daemon-disable-ascii-art"></a>
#### `--disable-ascii-art`

Disable the ASCII art shown at startup

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-disable-ascii-art` |
| Required | No |
| Value | No |
| Value name | `DISABLE_ASCII_ART` |
| Default value | `false` |
| Possible values | `true`, `false` |

<a id="xelis-daemon-datetime-format"></a>
#### `--datetime-format <DATETIME_FORMAT>`

Datetime format used in log entries

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-datetime-format` |
| Required | No |
| Value | Yes |
| Value name | `DATETIME_FORMAT` |
| Default value | `\[%Y-%m-%d\] (%H:%M:%S%.3f)` |

<a id="xelis-daemon-network"></a>
#### `--network <NETWORK>`

Network to run and validate

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-network` |
| Required | No |
| Value | Yes |
| Value name | `NETWORK` |
| Default value | `mainnet` |
| Possible values | `mainnet`, `testnet`, `stagenet`, `devnet` |

<a id="xelis-daemon-config-file"></a>
#### `--config-file <CONFIG_FILE>`

JSON configuration file to load

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-config-file` |
| Required | No |
| Value | Yes |
| Value name | `CONFIG_FILE` |

<a id="xelis-daemon-generate-config-template"></a>
#### `--generate-config-template`

Generate a configuration template at `config_file`

| Field | Value |
| --- | --- |
| Anchor | `#xelis-daemon-generate-config-template` |
| Required | No |
| Value | No |
| Value name | `GENERATE_CONFIG_TEMPLATE` |
| Default value | `false` |
| Possible values | `true`, `false` |
