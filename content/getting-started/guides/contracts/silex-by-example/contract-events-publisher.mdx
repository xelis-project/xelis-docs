---
title: Contract events publisher
description: Emit a contract-to-contract event.
---

## Contract events: publisher

**Focus:** one price publisher entry that emits a contract event for listeners.

[Open in Playground](https://playground.xelis.io/?code=Y29uc3QgUFJJQ0VfRVZFTlQ6IHU2NCA9IDEwMAoKZW50cnkgcHVibGlzaF9wcmljZShzeW1ib2w6IHN0cmluZywgcHJpY2U6IHU2NCkgewogICAgZW1pdF9ldmVudChQUklDRV9FVkVOVCwgW3N5bWJvbCwgcHJpY2VdKQogICAgcmV0dXJuIDAKfQ%3D%3D)

```rust filename="price_publisher.slx" copy
const PRICE_EVENT: u64 = 100

entry publish_price(symbol: string, price: u64) {
    emit_event(PRICE_EVENT, [symbol, price])
    return 0
}
```

## How it works

- `PRICE_EVENT` is the event id that other contracts will subscribe to.
- `emit_event(PRICE_EVENT, [...])` sends positional event data to contract listeners.
- The same event id and payload shape are used by the listener in the next example.
- The entry returns the default success exit code after publishing the event.

Deploy this first, then pass its contract hash to the listener in the next example.
